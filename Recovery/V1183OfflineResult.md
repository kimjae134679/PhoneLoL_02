# 1.18.3 로컬 1인 전투 결과 경로 보완

- 1.18.2의 선택형 로컬 모드는 로그인·URF 방 입장·챔피언 선택·월드 로딩까지 처리했지만 전투 종료 후 게임 서비스의 결과 조회 요청에는 미지원 응답을 보냈다.
- 오프라인 세션에서 전투가 시작한 시각과 월드 준비를 기록한다. 종료 패킷을 수신하면 로컬 결과를 생성하고 이전 서버와 같은 길이의 결과 조회 응답을 반환한다. 잘못된 게임 ID·챔피언 ID, 종료 전 조회는 명시적으로 거부한다.
- 로컬 결과는 그 기기의 실행 세션에서만 사용하고 계정·전적·랭킹 DB로 보내거나 동기화하지 않는다. 랭킹 목록은 로컬에서 비어 있는 목록으로 응답한다.
- 자동 검증 `Automation/VerifyV1183OfflineResult.cs`: 중앙 서버에 도달할 수 없는 조건에서 로컬 로그인/프로필/챔피언 목록/URF 방/전투 시작/월드 준비/종료 결과 제출/85바이트 결과 조회 및 잘못된 경기 ID 거부를 실제 로컬 소켓으로 확인했다.
- 1.18.2의 새 인터넷 경로 및 서버 설정은 유지한다. 실제 휴대폰의 터치·게임 화면·넥서스 파괴부터 종료 화면까지의 실전은 별도로 확인이 필요하다.

## 최종 Android 빌드

- `PhoneLOL-02/Builds/PhoneLOL-1.18.3.apk`: 143292982바이트, SHA-256 `EEA7ECEC4D7D9BBA07DC59B769453FA3FBBF5B41AA01E87A1646ED0A1FC1F159`.
- Unity Build Succeeded, errors 0, warnings 764. APK manifest versionName `1.18.3` / versionCode `207`; ARM64 라이브러리 6개; ZIP CRC 정상.
- 서버를 재시작하거나 온라인 계정 DB를 변경하지 않았다. 기존 사용자 `UnityConnectSettings.asset` 변경과 이전 BuildJob JSON은 커밋 대상에서 제외한다.
