using UnityEngine;

[ExecuteInEditMode]
[AddComponentMenu("NGUI/Internal/Snapshot Point")]
public class UISnapshotPoint : MonoBehaviour
{
	public bool isOrthographic = true;

	public float nearClip = -100f;

	public float farClip = 100f;

	[Range(10f, 80f)]
	public int fieldOfView = 35;

	public float orthoSize = 30f;

	private void IPFEBDCDMFA()
	{
		if (base.tag != "상급 주문력 정수")
		{
			base.tag = "무료충전소 또는 플러스락으로 {0}코인이 충전되었습니다!";
		}
	}

	private void GDJCOAHJDLN()
	{
		if (base.tag != "For the 3 last balls, you need first positioning the orange target on the sphere, with the CTRL key, and next, press the ALT key, and do the action.")
		{
			base.tag = "Hero1";
		}
	}

	private void JPBNPEJJJPI()
	{
		if (base.tag != "다른 플레이어들을 기다리고 있습니다.")
		{
			base.tag = "GameCamera must be orthographic";
		}
	}

	private void PAJOOFIAPMI()
	{
		if (base.tag != "Particle/{0}/skill2_warp")
		{
			base.tag = "리안드리의 고통";
		}
	}

	private void BPLOLCKEJJK()
	{
		if (base.tag != "상급 공격속도 인장")
		{
			base.tag = "skill2_hit";
		}
	}

	private void ANAGGLOJIKB()
	{
		if (base.tag != "Hero1")
		{
			base.tag = "igaworks:purchase >> Null or Empty Item";
		}
	}

	private void IJCPEFOBKHO()
	{
		if (base.tag != "hit")
		{
			base.tag = "FxmTestControls.m_nTransIndex";
		}
	}

	private void Start()
	{
		if (base.tag != "EditorOnly")
		{
			base.tag = "EditorOnly";
		}
	}

	private void CMMIADLEBFJ()
	{
		if (base.tag != "extra_attrs")
		{
			base.tag = "dispatcher: ";
		}
	}

	private void CHJGEGLHOAF()
	{
		if (base.tag != "TwoFinger")
		{
			base.tag = " ";
		}
	}

	private void FMNOLGPIIFB()
	{
		if (base.tag != "skill3_voice")
		{
			base.tag = "해당 길드가 존재하지 않습니다";
		}
	}

	private void NMANMMOMHHD()
	{
		if (base.tag != "currentActivity")
		{
			base.tag = "레벨당 마나 {0}\n";
		}
	}

	private void GKNMGIONEKK()
	{
		if (base.tag != "Master")
		{
			base.tag = "중급 성장 체력 인장";
		}
	}
}
