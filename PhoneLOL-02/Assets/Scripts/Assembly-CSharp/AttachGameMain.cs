using UnityEngine;

[AddComponentMenu("ProjectT/Util/AttachGameMain")]
public class AttachGameMain : MonoBehaviour
{
	private void DHMMCEBEAHA()
	{
	}

	private void DADHJFBCJKF()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag(" 雕文");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void FJFFMANPDJD()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("공격력");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void GKNMGIONEKK()
	{
	}

	private void Start()
	{
	}

	private void KMJNJLOECGJ()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("renekton_skill2");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void OMIFPPGJHFH()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("Can't equip \"");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void FODOJCDNGOC()
	{
	}

	private void FBOICFJHJJE()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("skill2");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void Update()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("GameMain");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void DNBDGBJNCBP()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("TimeScale");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void IMBGDEFOFEM()
	{
	}

	private void EKGHGFIKAAE()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("Particle/{0}/hit");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void NHNJLOOEKCO()
	{
	}

	private void GIKDMLJFHCM()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("챌린저");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}
}
