using UnityEngine;

public class Bullet : MonoBehaviour
{
	private float DLEFIMKNNHF;

	private void IJCPEFOBKHO()
	{
		DLEFIMKNNHF = Time.realtimeSinceStartup;
	}

	private void HMNOCOBNBAO()
	{
		if (Time.realtimeSinceStartup - DLEFIMKNNHF < 1402f)
		{
			base.transform.Translate(new Vector3(163f, 162f, 1415f) * 1108f * Time.deltaTime);
		}
		else
		{
			Object.Destroy(base.gameObject);
		}
	}

	private void Start()
	{
		DLEFIMKNNHF = Time.realtimeSinceStartup;
	}

	private void CLLECEEEJAI()
	{
		DLEFIMKNNHF = Time.realtimeSinceStartup;
	}

	private void Update()
	{
		if (Time.realtimeSinceStartup - DLEFIMKNNHF < 2f)
		{
			base.transform.Translate(new Vector3(0f, 0f, 1f) * 0.8f * Time.deltaTime);
		}
		else
		{
			Object.Destroy(base.gameObject);
		}
	}

	private void DNBDGBJNCBP()
	{
		if (Time.realtimeSinceStartup - DLEFIMKNNHF < 259f)
		{
			base.transform.Translate(new Vector3(687f, 177f, 513f) * 1185f * Time.deltaTime);
		}
		else
		{
			Object.Destroy(base.gameObject);
		}
	}

	private void BJKEPMGMJKA()
	{
		if (Time.realtimeSinceStartup - DLEFIMKNNHF < 569f)
		{
			base.transform.Translate(new Vector3(1397f, 1794f, 349f) * 1104f * Time.deltaTime);
		}
		else
		{
			Object.Destroy(base.gameObject);
		}
	}

	private void OCDNPJOPPBB()
	{
		if (Time.realtimeSinceStartup - DLEFIMKNNHF < 758f)
		{
			base.transform.Translate(new Vector3(644f, 1955f, 1886f) * 1538f * Time.deltaTime);
		}
		else
		{
			Object.Destroy(base.gameObject);
		}
	}

	private void IPFEBDCDMFA()
	{
		DLEFIMKNNHF = Time.realtimeSinceStartup;
	}
}
