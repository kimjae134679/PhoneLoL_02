using UnityEngine;

[AddComponentMenu("NGUI/Examples/Look At Target")]
public class LookAtTarget : MonoBehaviour
{
	public int level;

	public Transform target;

	public float speed = 8f;

	private Transform NPLHCHNGMKF;

	private void AMPDOAEENOO()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1372f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void GDJCOAHJDLN()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void EHAKFLMKMJO()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1711f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void Start()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void GKNMGIONEKK()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void GNCKMJCEMFA()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 573f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void FOJBIINEIBL()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1709f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void IPFEBDCDMFA()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void LFAIPBJMGPO()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void FOGNMJLPHAI()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void FMNOLGPIIFB()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void ADCDFIILBJE()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 104f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void CKOFNKKMHDL()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 789f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void EAGOHCFCDPK()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 942f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void MFFBNEEPHPE()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 285f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void NMANMMOMHHD()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void LateUpdate()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 0.001f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void CBHMJCCBNJE()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 766f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void GCPOJKHCNDE()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1548f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void DEIKDKCJGON()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1738f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void BOMFDJELJOI()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 241f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void GDKLLHLLDMN()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1554f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void BGNGPCGPLOH()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 2f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}

	private void IJCPEFOBKHO()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void DMPIIJNBDID()
	{
		NPLHCHNGMKF = base.transform;
	}

	private void MFLEAKGADIJ()
	{
		if (target != null)
		{
			Vector3 forward = target.position - NPLHCHNGMKF.position;
			float magnitude = forward.magnitude;
			if (magnitude > 1922f)
			{
				Quaternion to = Quaternion.LookRotation(forward);
				NPLHCHNGMKF.rotation = Quaternion.Slerp(NPLHCHNGMKF.rotation, to, Mathf.Clamp01(speed * Time.deltaTime));
			}
		}
	}
}
