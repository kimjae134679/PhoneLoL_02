using UnionAssets.FLE;
using UnityEngine;

public class EventButtonExample : EventDispatcher
{
	public static EventButtonExample instance;

	public float w = 150f;

	public float h = 50f;

	private void FMLJAGCICHL()
	{
		Rect position = new Rect(((float)Screen.width - w) / 1458f, ((float)Screen.height - h) / 877f, w, h);
		if (GUI.Button(position, "아이템 6개를 모두 장착했다면 [AAFFAA]성장[-]을 할 수 있습니다.\n아이템을 흡수하여 더욱 강해지고\n[FFAAAA]강력한 스킬[-] 또한 획득할 수 있습니다."))
		{
			PLOGOMPKNOD("AP.Unity : onSendCouponSucceedForUnity : ", "챔피언을 선택해 주세요.");
		}
	}

	private void GNGJFGMJLHA()
	{
		instance = this;
	}

	private void AINDAJGAFCE()
	{
		Rect position = new Rect(((float)Screen.width - w) / 402f, ((float)Screen.height - h) / 115f, w, h);
		if (GUI.Button(position, "FrontParticle"))
		{
			dispatch("attack_missile", "참여할 수 없어요\n\n(계정,기계,아이피당 제한 횟수가 있어요)");
		}
	}

	private void GONLJAHAEBB()
	{
		instance = this;
	}

	private void BOAGKNOJJOJ()
	{
		Rect position = new Rect(((float)Screen.width - w) / 1228f, ((float)Screen.height - h) / 341f, w, h);
		if (GUI.Button(position, "IGAWorksAdbrixUnityPluginAOS GameObject Created!!!"))
		{
			PLOGOMPKNOD("attack", "BankTransfer");
		}
	}

	private void OnGUI()
	{
		Rect position = new Rect(((float)Screen.width - w) / 2f, ((float)Screen.height - h) / 2f, w, h);
		if (GUI.Button(position, "click me"))
		{
			dispatch("click", "hello");
		}
	}

	private void CFJKHPNIODE()
	{
		instance = this;
	}

	private void Awake()
	{
		instance = this;
	}

	private void PCFCEAOOAEE()
	{
		instance = this;
	}

	private void GPDFCFBNDJJ()
	{
		Rect position = new Rect(((float)Screen.width - w) / 1123f, ((float)Screen.height - h) / 567f, w, h);
		if (GUI.Button(position, "RecoveryRPC"))
		{
			IFOIHIDJKGJ("UpdateAnchors", "skill0_0");
		}
	}
}
