using System;
using System.Runtime.CompilerServices;

namespace EveEngine
{
	public class BLKHDLFFJML
	{
		private byte[] FCDMNFOILLC;

		public short ICENKPDOHBK
		{
			get
			{
				return OAOOIELFDOI();
			}
			set
			{
				FNJJKALMMDD(value);
			}
		}

		[SpecialName]
		public void PDGBNLLOEJI(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[1];
			for (int i = 1; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 0, 5, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void EJFDKKEEEIG(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[4];
			for (int i = 1; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.NEGOIEMLELI(array, 1, 6, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		public void FNJJKALMMDD(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[8];
			for (int i = 0; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 0, 8, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public short FJCBCCMEHAM()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 0, 3, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 1);
		}

		public short OAOOIELFDOI()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 0, 8, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 0);
		}

		[SpecialName]
		public short GKIOOEFIOID()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 1, 4, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 1);
		}

		[SpecialName]
		public short NGLHDJLEGHJ()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 0, 3, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 0);
		}

		[SpecialName]
		public short LHECGEMDJMH()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.HHKPMIELCND(array, 0, 1, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 0);
		}

		[SpecialName]
		public void GPOPEFLHBEF(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[4];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.GKOEPAHPMCK(array, 1, 8, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void ANGJBEKAAJL(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[6];
			for (int i = 1; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.NEGOIEMLELI(array, 0, 1, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public short KNHCGGNDLDP()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 1, 0, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 1);
		}

		[SpecialName]
		public void IEMDADIGAIL(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[4];
			for (int i = 1; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.GKOEPAHPMCK(array, 0, 0, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void AGBKAJNBEGO(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[8];
			for (int i = 1; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.CIBLPPNNOKM(array, 1, 1, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		public BLKHDLFFJML()
		{
			FCDMNFOILLC = new byte[8];
			FNJJKALMMDD(0);
		}

		[SpecialName]
		public void MEMNMJBHFJE(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[1];
			for (int i = 1; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.CIBLPPNNOKM(array, 1, 4, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public short DECIBPPMKKF()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.HHKPMIELCND(array, 1, 5, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToInt16(array, 1);
		}

		[SpecialName]
		public void HFJJJODCNNK(short ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[6];
			for (int i = 0; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 1, 5, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}
	}
}
