using System;
using System.Runtime.CompilerServices;

namespace EveEngine
{
	public class EOEEIDIGIFJ
	{
		private byte[] FCDMNFOILLC;

		public uint ICENKPDOHBK
		{
			get
			{
				return OAOOIELFDOI();
			}
			set
			{
				FNJJKALMMDD(value);
			}
		}

		[SpecialName]
		public void FOBPGMDAIDB(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[8];
			for (int i = 1; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.GKOEPAHPMCK(array, 0, 1, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public uint MDMHGMGPKCL()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 0, 3, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		public EOEEIDIGIFJ()
		{
			FCDMNFOILLC = new byte[8];
			FNJJKALMMDD(0u);
		}

		[SpecialName]
		public uint GGLOBOCFFPP()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.HHKPMIELCND(array, 0, 2, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		[SpecialName]
		public uint DECIBPPMKKF()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 0, 0, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		[SpecialName]
		public uint OLJNCMIKHDD()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 1, 1, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		[SpecialName]
		public void ANGJBEKAAJL(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[1];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.NEGOIEMLELI(array, 0, 2, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void INGGBCLDOGO(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[2];
			for (int i = 0; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.GKOEPAHPMCK(array, 1, 0, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public uint PEJFJIAGMKF()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.HHKPMIELCND(array, 0, 2, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		[SpecialName]
		public void LLMMCELOHMG(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[0];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.NEGOIEMLELI(array, 0, 5, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void ONHLCIELNKM(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[4];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 1, 2, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void DLOFJAJAAED(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[5];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 1, 8, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		public uint OAOOIELFDOI()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 0, 8, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 0);
		}

		public void FNJJKALMMDD(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[8];
			for (int i = 0; i < bytes.Length; i++)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 0, 8, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public uint MAJCEDCCALB()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.HHKPMIELCND(array, 0, 8, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		[SpecialName]
		public void IFGJFOHIGEM(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[4];
			for (int i = 1; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.GKOEPAHPMCK(array, 1, 5, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void MEMNMJBHFJE(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[3];
			for (int i = 1; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.CIBLPPNNOKM(array, 1, 1, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public uint ALEDGAAHLAP()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.OGMPIEONIIN(array, 1, 1, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 1);
		}

		[SpecialName]
		public void EJFDKKEEEIG(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[8];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.CIBLPPNNOKM(array, 0, 7, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void LAAKIDMLCFA(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[2];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.GKOEPAHPMCK(array, 1, 4, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j++)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void LIHGEOPAHDL(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[5];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.CIBLPPNNOKM(array, 0, 0, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 1; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public void AGBKAJNBEGO(uint ICENKPDOHBK)
		{
			byte[] bytes = BitConverter.GetBytes(ICENKPDOHBK);
			byte[] array = new byte[0];
			for (int i = 0; i < bytes.Length; i += 0)
			{
				array[i] = bytes[i];
			}
			EEPLIIONNPN.FHGLAHMGHIC(array, 0, 4, MNPFIKCCDMH.AKKALMDAFGH);
			for (int j = 0; j < FCDMNFOILLC.Length; j += 0)
			{
				FCDMNFOILLC[j] = array[j];
			}
		}

		[SpecialName]
		public uint ACADLPBCDJK()
		{
			byte[] array = (byte[])FCDMNFOILLC.Clone();
			EEPLIIONNPN.HHKPMIELCND(array, 0, 5, MNPFIKCCDMH.AKKALMDAFGH);
			return BitConverter.ToUInt32(array, 0);
		}
	}
}
