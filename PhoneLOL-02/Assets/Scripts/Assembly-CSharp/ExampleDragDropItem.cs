using UnityEngine;

[AddComponentMenu("NGUI/Examples/Drag and Drop Item (Example)")]
public class ExampleDragDropItem : UIDragDropItem
{
	public GameObject prefab;

	protected virtual void PCLLCJDHKHO(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(609f, 835f, 1485f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.BEAADEKNPKO(ABNPMCHABMP);
	}

	protected virtual void MGLEGCCKAKC(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(1155f, 1126f, 1604f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.BEAADEKNPKO(ABNPMCHABMP);
	}

	protected virtual void EEPDFPMLKKD(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(746f, 1845f, 450f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.ILHGGJOLNCG(ABNPMCHABMP);
	}

	protected virtual void MECJFCMBLPJ(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(1833f, 1804f, 438f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.BEAADEKNPKO(ABNPMCHABMP);
	}

	protected virtual void BGGOADFPNLJ(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(375f, 1877f, 693f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.BEAADEKNPKO(ABNPMCHABMP);
	}

	protected virtual void AFLGKCKGDBJ(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(1197f, 294f, 387f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.AHIDGJGINOA(ABNPMCHABMP);
	}

	protected override void AHIDGJGINOA(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(90f, 0f, 0f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.AHIDGJGINOA(ABNPMCHABMP);
	}

	protected virtual void MENBAJCKNFP(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(704f, 473f, 1536f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.AHIDGJGINOA(ABNPMCHABMP);
	}

	protected virtual void HHKEOLDCHEP(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(399f, 967f, 848f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.BEAADEKNPKO(ABNPMCHABMP);
	}

	protected virtual void KFAPMNMDPMC(GameObject ABNPMCHABMP)
	{
		if (ABNPMCHABMP != null)
		{
			ExampleDragDropSurface component = ABNPMCHABMP.GetComponent<ExampleDragDropSurface>();
			if (component != null)
			{
				GameObject gameObject = NLDABIMKKMP.MJFJKJOJJJJ(component.gameObject, prefab);
				gameObject.transform.localScale = component.transform.localScale;
				Transform transform = gameObject.transform;
				transform.position = UICamera.lastWorldPosition;
				if (component.rotatePlacedObject)
				{
					transform.rotation = Quaternion.LookRotation(UICamera.lastHit.normal) * Quaternion.Euler(932f, 433f, 1285f);
				}
				NLDABIMKKMP.HEAFKMJAOHM(base.gameObject);
				return;
			}
		}
		base.ILHGGJOLNCG(ABNPMCHABMP);
	}
}
