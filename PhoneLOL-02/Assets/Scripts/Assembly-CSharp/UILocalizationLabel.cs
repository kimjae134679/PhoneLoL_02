using UnityEngine;

[AddComponentMenu("ProjectT/UI/Util/UILocalizationLabel")]
public class UILocalizationLabel : MonoBehaviour
{
	private void GNGJFGMJLHA()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.LGDEHELDENG().GetText(component.get_text()));
		}
	}

	private void JGFFKGLIKBC()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.set_text(LocalizationManager.LGDEHELDENG().GetText(component.get_text()));
		}
	}

	private void PCFCEAOOAEE()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.set_text(LocalizationManager.get_Instance().GetText(component.get_text()));
		}
	}

	private void LBAPOJHKDNH()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.set_text(LocalizationManager.LGDEHELDENG().GetText(component.get_text()));
		}
	}

	private void HKAMCNPENHB()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.LGDEHELDENG().GetText(component.get_text()));
		}
	}

	private void EICMMILMBPA()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.get_Instance().GetText(component.get_text()));
		}
	}

	private void DEFLIOMCCMA()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.get_Instance().GetText(component.get_text()));
		}
	}

	private void MPKBLEDGKPO()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.set_text(LocalizationManager.get_Instance().GetText(component.get_text()));
		}
	}

	private void Awake()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.set_text(LocalizationManager.get_Instance().GetText(component.get_text()));
		}
	}

	private void FPFNIFKNOIF()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.LGDEHELDENG().GetText(component.get_text()));
		}
	}

	private void MKCBHHEACDE()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.LGDEHELDENG().GetText(component.get_text()));
		}
	}

	private void NGNNLLLBGMB()
	{
		UILabel component = GetComponent<UILabel>();
		if (!(component == null))
		{
			component.DCKJILCPKFD(LocalizationManager.get_Instance().GetText(component.get_text()));
		}
	}
}
