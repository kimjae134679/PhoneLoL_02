using UnityEngine;

[AddComponentMenu("ProjectT/Util/AttachGameMap")]
public class AttachGameMap : MonoBehaviour
{
	private void FJFFMANPDJD()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("C#: HandleSetUserIDSuccess");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void Start()
	{
	}

	private void GDKGBNKCGHO()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("attack_missile");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void Update()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("GameMap");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void OCDNPJOPPBB()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("talon_skill0_blood");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void GOLOMEGDPAH()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("[");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void PEIFAOJLDMD()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("1 finger => Look around");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void JPBNPEJJJPI()
	{
	}

	private void EKGHGFIKAAE()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("Replay");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void HMNOCOBNBAO()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("Free camera ctrl or alt key to simulate the second finger");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}

	private void DNBDGBJNCBP()
	{
		if (base.transform.parent == null)
		{
			Vector3 position = base.transform.position;
			Vector3 localScale = base.transform.localScale;
			GameObject gameObject = GameObject.FindGameObjectWithTag("\" : ");
			base.transform.parent = gameObject.transform;
			base.transform.localPosition = position;
			base.transform.localScale = localScale;
		}
	}
}
