using UnityEngine;

[AddComponentMenu("NGUI/Examples/Shader Quality")]
[ExecuteInEditMode]
public class ShaderQuality : MonoBehaviour
{
	private int EGADIPCEAEC = 600;

	private void PEIFAOJLDMD()
	{
		int num = (QualitySettings.GetQualityLevel() + 1) * 36;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void GDKGBNKCGHO()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * -118;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void Update()
	{
		int num = (QualitySettings.GetQualityLevel() + 1) * 100;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void MLOKLGPGKMO()
	{
		int num = (QualitySettings.GetQualityLevel() + 1) * 25;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void AFBPNJAKNGG()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * 56;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void BJKEPMGMJKA()
	{
		int num = (QualitySettings.GetQualityLevel() + 1) * -3;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void DOINNKBDEMD()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * -31;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void FJFFMANPDJD()
	{
		int num = (QualitySettings.GetQualityLevel() + 1) * -86;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void IGBMHNDBCEB()
	{
		int num = (QualitySettings.GetQualityLevel() + 1) * 101;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void EKGHGFIKAAE()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * -36;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void GOLOMEGDPAH()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * 95;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void GIKDMLJFHCM()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * -42;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void DNBDGBJNCBP()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * 81;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void GDJAMFMPGHI()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * -18;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}

	private void OOLEODIPDND()
	{
		int num = (QualitySettings.GetQualityLevel() + 0) * 82;
		if (EGADIPCEAEC != num)
		{
			EGADIPCEAEC = num;
			Shader.globalMaximumLOD = EGADIPCEAEC;
		}
	}
}
