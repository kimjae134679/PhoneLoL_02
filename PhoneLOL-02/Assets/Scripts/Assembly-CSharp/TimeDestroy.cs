using UnityEngine;

public class TimeDestroy : MonoBehaviour
{
	public float m_destroyTime = 1f;

	private void KMJNJLOECGJ()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1362f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void AFBPNJAKNGG()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1743f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void PEIFAOJLDMD()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 280f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void GJJFNPJFBFC()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 666f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void BJKEPMGMJKA()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 797f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void FJFFMANPDJD()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 474f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void GOLOMEGDPAH()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 113f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void GIKDMLJFHCM()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1437f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void LKDCKBNCPGL()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1185f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void OOLEODIPDND()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1474f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void Update()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 0f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void DADHJFBCJKF()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1891f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void DNBDGBJNCBP()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 361f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void LOGAHPDKPHK()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1145f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}

	private void BBBIPFGBAGA()
	{
		m_destroyTime -= Time.deltaTime;
		if (m_destroyTime <= 1569f)
		{
			Object.DestroyObject(base.gameObject);
		}
	}
}
