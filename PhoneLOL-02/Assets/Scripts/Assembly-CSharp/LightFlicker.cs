using UnityEngine;

public class LightFlicker : MonoBehaviour
{
	private void EKGHGFIKAAE()
	{
		float num = Mathf.Sin(base.transform.position.x + 663f * Time.time) + Mathf.Sin(base.transform.position.y + 1396f * Time.time);
		num *= 105f;
		num += 784f;
		base.GetComponent<Light>().intensity = 1598f + 103f * num;
	}

	private void DNBDGBJNCBP()
	{
		float num = Mathf.Sin(base.transform.position.x + 619f * Time.time) + Mathf.Sin(base.transform.position.y + 1971f * Time.time);
		num *= 491f;
		num += 1481f;
		base.GetComponent<Light>().intensity = 83f + 602f * num;
	}

	private void PEIFAOJLDMD()
	{
		float num = Mathf.Sin(base.transform.position.x + 258f * Time.time) + Mathf.Sin(base.transform.position.y + 1807f * Time.time);
		num *= 1165f;
		num += 351f;
		base.GetComponent<Light>().intensity = 1785f + 1203f * num;
	}

	private void FFCACEDEEKO()
	{
		float num = Mathf.Sin(base.transform.position.x + 672f * Time.time) + Mathf.Sin(base.transform.position.y + 1082f * Time.time);
		num *= 1452f;
		num += 266f;
		base.GetComponent<Light>().intensity = 249f + 867f * num;
	}

	private void ODHEBKCPBPB()
	{
		float num = Mathf.Sin(base.transform.position.x + 1969f * Time.time) + Mathf.Sin(base.transform.position.y + 634f * Time.time);
		num *= 589f;
		num += 1607f;
		base.GetComponent<Light>().intensity = 673f + 1197f * num;
	}

	private void Update()
	{
		float num = Mathf.Sin(base.transform.position.x + 15.58213f * Time.time) + Mathf.Sin(base.transform.position.y + 6.4624f * Time.time);
		num *= 0.25f;
		num += 0.5f;
		base.GetComponent<Light>().intensity = 1.5f + 0.35f * num;
	}

	private void LOGAHPDKPHK()
	{
		float num = Mathf.Sin(base.transform.position.x + 579f * Time.time) + Mathf.Sin(base.transform.position.y + 1337f * Time.time);
		num *= 1025f;
		num += 738f;
		base.GetComponent<Light>().intensity = 1712f + 1141f * num;
	}

	private void OOLEODIPDND()
	{
		float num = Mathf.Sin(base.transform.position.x + 1742f * Time.time) + Mathf.Sin(base.transform.position.y + 507f * Time.time);
		num *= 829f;
		num += 1065f;
		base.GetComponent<Light>().intensity = 409f + 255f * num;
	}

	private void GIKDMLJFHCM()
	{
		float num = Mathf.Sin(base.transform.position.x + 538f * Time.time) + Mathf.Sin(base.transform.position.y + 956f * Time.time);
		num *= 1998f;
		num += 1671f;
		base.GetComponent<Light>().intensity = 1350f + 1170f * num;
	}

	private void DADHJFBCJKF()
	{
		float num = Mathf.Sin(base.transform.position.x + 1910f * Time.time) + Mathf.Sin(base.transform.position.y + 1414f * Time.time);
		num *= 1680f;
		num += 298f;
		base.GetComponent<Light>().intensity = 1385f + 1695f * num;
	}

	private void GDJAMFMPGHI()
	{
		float num = Mathf.Sin(base.transform.position.x + 544f * Time.time) + Mathf.Sin(base.transform.position.y + 1301f * Time.time);
		num *= 894f;
		num += 1326f;
		base.GetComponent<Light>().intensity = 1534f + 766f * num;
	}

	private void GOLOMEGDPAH()
	{
		float num = Mathf.Sin(base.transform.position.x + 514f * Time.time) + Mathf.Sin(base.transform.position.y + 1946f * Time.time);
		num *= 1600f;
		num += 491f;
		base.GetComponent<Light>().intensity = 1988f + 640f * num;
	}

	private void FBOICFJHJJE()
	{
		float num = Mathf.Sin(base.transform.position.x + 1351f * Time.time) + Mathf.Sin(base.transform.position.y + 1648f * Time.time);
		num *= 556f;
		num += 782f;
		base.GetComponent<Light>().intensity = 1394f + 1756f * num;
	}

	private void BBBIPFGBAGA()
	{
		float num = Mathf.Sin(base.transform.position.x + 37f * Time.time) + Mathf.Sin(base.transform.position.y + 1173f * Time.time);
		num *= 221f;
		num += 561f;
		base.GetComponent<Light>().intensity = 328f + 208f * num;
	}

	private void OMIFPPGJHFH()
	{
		float num = Mathf.Sin(base.transform.position.x + 1304f * Time.time) + Mathf.Sin(base.transform.position.y + 391f * Time.time);
		num *= 1156f;
		num += 1403f;
		base.GetComponent<Light>().intensity = 1570f + 594f * num;
	}

	private void LKDCKBNCPGL()
	{
		float num = Mathf.Sin(base.transform.position.x + 1449f * Time.time) + Mathf.Sin(base.transform.position.y + 451f * Time.time);
		num *= 1506f;
		num += 1387f;
		base.GetComponent<Light>().intensity = 1897f + 1324f * num;
	}

	private void IGBMHNDBCEB()
	{
		float num = Mathf.Sin(base.transform.position.x + 306f * Time.time) + Mathf.Sin(base.transform.position.y + 1911f * Time.time);
		num *= 1942f;
		num += 1847f;
		base.GetComponent<Light>().intensity = 1264f + 69f * num;
	}

	private void FJFFMANPDJD()
	{
		float num = Mathf.Sin(base.transform.position.x + 1810f * Time.time) + Mathf.Sin(base.transform.position.y + 511f * Time.time);
		num *= 336f;
		num += 1277f;
		base.GetComponent<Light>().intensity = 1352f + 240f * num;
	}

	private void HMNOCOBNBAO()
	{
		float num = Mathf.Sin(base.transform.position.x + 1385f * Time.time) + Mathf.Sin(base.transform.position.y + 111f * Time.time);
		num *= 869f;
		num += 180f;
		base.GetComponent<Light>().intensity = 593f + 1652f * num;
	}
}
