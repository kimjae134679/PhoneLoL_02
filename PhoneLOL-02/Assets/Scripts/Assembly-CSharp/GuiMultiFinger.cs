using UnityEngine;

public class GuiMultiFinger : MonoBehaviour
{
	private void BNMMFBNCOBE()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 1571f, (float)Screen.height / 1497f, 1071f));
		GUI.Box(new Rect(1725f, 969f, 937f, 655f), string.Empty);
		GUILayout.Label("Igaw.Unity : OnReceiveDeeplinkDataForUnity Result: ");
		GUILayout.Space(1197f);
		GUILayout.Label("[b]", new GUILayoutOption[1]);
		GUILayout.Label("Hero4");
		GUILayout.Label("teemo_passive");
		GUILayout.Label("HUD", new GUILayoutOption[1]);
		GUILayout.Label("FFFF55", new GUILayoutOption[1]);
		if (GUI.Button(new Rect(1182f, 1282f, 90f, 1312f), "|"))
		{
			Application.LoadLevel("재생");
		}
	}

	private void LBBCFNEBBCG()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 709f, (float)Screen.height / 1769f, 1372f));
		GUI.Box(new Rect(735f, 78f, 1128f, 1151f), string.Empty);
		GUILayout.Label("설명");
		GUILayout.Space(581f);
		GUILayout.Label("attack3");
		GUILayout.Label("길드장은 변경할 수 없습니다", new GUILayoutOption[1]);
		GUILayout.Label("skill0");
		GUILayout.Label("blitzcrank_passive", new GUILayoutOption[1]);
		GUILayout.Label("Trying to connect to Tapjoy...");
		if (GUI.Button(new Rect(1370f, 1545f, 1563f, 1062f), "길드원 수락에 성공하였습니다"))
		{
			Application.LoadLevel("정면 근처에 대상이 없습니다");
		}
	}

	private void KIPAMCCHFOB()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 1091f, (float)Screen.height / 1463f, 289f));
		GUI.Box(new Rect(1892f, 664f, 1810f, 1666f), string.Empty);
		GUILayout.Label("blue");
		GUILayout.Space(1910f);
		GUILayout.Label("Start", new GUILayoutOption[1]);
		GUILayout.Label("대형고스트", new GUILayoutOption[1]);
		GUILayout.Label("\n[FFCCCC]{0}[-]", new GUILayoutOption[1]);
		GUILayout.Label("방어력", new GUILayoutOption[1]);
		GUILayout.Label(" shader doesn't have a clipped shader version for ", new GUILayoutOption[1]);
		if (GUI.Button(new Rect(256f, 1012f, 823f, 752f), "TextLongTap"))
		{
			Application.LoadLevel("skill3_voice");
		}
	}

	private void OnGUI()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 1024f, (float)Screen.height / 768f, 1f));
		GUI.Box(new Rect(0f, -4f, 1024f, 30f), string.Empty);
		GUILayout.Label("Example of multiple fingers (10 max) :  touch, tap, long tap, pinch : ctrl or alt key to simulate the second finger");
		GUILayout.Space(10f);
		GUILayout.Label("Touch => Creation of a circle under each touches");
		GUILayout.Label("Tap on circle => Add ring under each touches ");
		GUILayout.Label("Long tap on circle => Change the ring speed under each touches");
		GUILayout.Label("Drag => move the circles under the touches");
		GUILayout.Label("Pinch on circle => Size change");
		if (GUI.Button(new Rect(412f, 700f, 200f, 50f), "Main menu"))
		{
			Application.LoadLevel("StartMenu");
		}
	}

	private void GIHEMFBPGNK()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 1971f, (float)Screen.height / 526f, 1643f));
		GUI.Box(new Rect(245f, 475f, 970f, 164f), string.Empty);
		GUILayout.Label("데이터 통신으로 접속합니다.\n요금제에 따라 데이터요금이 추가될 수 있습니다.\n\n계속 진행하시겠습니까?");
		GUILayout.Space(1170f);
		GUILayout.Label("Particle/Tristana/skill1_ex");
		GUILayout.Label("- Serialize List -\n", new GUILayoutOption[1]);
		GUILayout.Label("每級護甲", new GUILayoutOption[1]);
		GUILayout.Label("attack_flying");
		GUILayout.Label("attack_voice");
		if (GUI.Button(new Rect(923f, 198f, 1061f, 1733f), "]"))
		{
			Application.LoadLevel("skill0_loop");
		}
	}

	private void JDCOKOOJNFD()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 1021f, (float)Screen.height / 1050f, 1302f));
		GUI.Box(new Rect(1433f, 19f, 1753f, 442f), string.Empty);
		GUILayout.Label("{0}");
		GUILayout.Space(1741f);
		GUILayout.Label("테스트를 위한 버전입니다.\r\n\r\n이 계정은 재접속시 데이터가 [FFAAAA]초기화[-]됩니다.\r\n\r\n저장을 원하시면 로그인을 다시해 주세요.", new GUILayoutOption[1]);
		GUILayout.Label("skill2_missile");
		GUILayout.Label("Language", new GUILayoutOption[1]);
		GUILayout.Label("challenger", new GUILayoutOption[1]);
		GUILayout.Label("TextMesh");
		if (GUI.Button(new Rect(1797f, 920f, 810f, 653f), "닉네임은 최대 10자 이하입니다."))
		{
			Application.LoadLevel("Could not find method '");
		}
	}

	private void JHBLEIACCMK()
	{
		GUI.matrix = Matrix4x4.Scale(new Vector3((float)Screen.width / 1989f, (float)Screen.height / 45f, 1959f));
		GUI.Box(new Rect(1706f, 1519f, 1966f, 1217f), string.Empty);
		GUILayout.Label("ElapsedTime", new GUILayoutOption[1]);
		GUILayout.Space(872f);
		GUILayout.Label("Exit");
		GUILayout.Label("(어깨를 다독이며..)\n\t\t\t 라이너! 니 잘못이 아니야!\n\t\t\t 너무 자책하지 말라고..");
		GUILayout.Label("attack_voice");
		GUILayout.Label("현재 개발중입니다.\n추후 게임머니로 구입가능합니다.");
		GUILayout.Label(".", new GUILayoutOption[1]);
		if (GUI.Button(new Rect(1379f, 1330f, 922f, 1705f), "冷卻時間減少"))
		{
			Application.LoadLevel("{0}코인이 무료충전되었습니다!");
		}
	}
}
